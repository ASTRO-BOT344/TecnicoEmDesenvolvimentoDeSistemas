atributo = é a característica que se da ao objeto

classe = é como um molde que vai estar atrelado ao metodo e objeto

construtor = é o comando que vai iniciar os atributos do objeto

Método = são as ações ou comportamentos de um objeto

Objeto = é uma coisa concreta criada a partir do molde(classe)